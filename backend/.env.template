# ====================================
# WSOP Field Director Pro - 환경 변수 템플릿
# ====================================
# 이 파일을 복사하여 .env.local 또는 .env.production으로 저장하세요
# Vercel 대시보드에서 이 값들을 입력하세요

# ====================================
# 데이터베이스 설정
# ====================================
# Vercel Postgres를 생성하면 자동으로 제공되는 URL
DATABASE_URL="postgres://default:xxxxx@xxx.postgres.vercel-storage.com:5432/verceldb?pgbouncer=true&connect_timeout=15"

# ====================================
# 인증 및 보안
# ====================================
# 최소 32자 이상의 안전한 랜덤 문자열 사용
# 생성 명령: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secure-jwt-secret-key-change-this-immediately"

# JWT 토큰 만료 시간
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# ====================================
# 애플리케이션 설정
# ====================================
# 환경 (development, production, test)
NODE_ENV="production"

# 프론트엔드 URL (CORS 허용을 위해 필요)
FRONTEND_URL="https://your-app.vercel.app"

# 타임존 설정
DEFAULT_TIMEZONE="Asia/Seoul"
TOURNAMENT_TIMEZONE="Europe/Athens"

# 앱 정보
APP_NAME="WSOP Field Director Pro"
APP_VERSION="1.0.0"

# ====================================
# 선택사항: 이메일 서비스
# ====================================
# SendGrid 사용 시
# EMAIL_SERVICE="sendgrid"
# SENDGRID_API_KEY="your-sendgrid-api-key"
# EMAIL_FROM="noreply@wsop-field-director.com"

# ====================================
# 선택사항: 파일 저장소 (AWS S3)
# ====================================
# AWS_ACCESS_KEY_ID="your-aws-access-key"
# AWS_SECRET_ACCESS_KEY="your-aws-secret-key"
# AWS_REGION="ap-northeast-2"
# AWS_S3_BUCKET="wsop-field-director-uploads"

# ====================================
# 선택사항: 모니터링
# ====================================
# Sentry 에러 트래킹
# SENTRY_DSN="https://xxxxx@sentry.io/xxxxx"

# 로그 레벨 (debug, info, warn, error)
LOG_LEVEL="info"

# ====================================
# 개발 환경 전용 (프로덕션에서는 사용 금지)
# ====================================
# ENABLE_DEBUG_MODE="false"
# DISABLE_AUTH="false"